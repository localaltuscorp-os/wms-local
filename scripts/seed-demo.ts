// Disabled for Altus Corp — no demo data. Real employees are invited via
// /admin/employees once the first admin is bootstrapped.

console.log("Demo seed disabled — Altus Corp starts with empty data.");
process.exit(0);
