/**
 * HR Support domain event barrel. The concrete builders live in
 * `hr-ticket-events.ts` (created by the foundation); this file is the stable
 * import surface for the HR module so call sites can `@/lib/events/hr-events`.
 */
export * from "./hr-ticket-events";
